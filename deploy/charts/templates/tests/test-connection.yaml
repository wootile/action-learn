apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wecom-bot.fullname" . }}-test-connection"
  labels:
    {{- include "wecom-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wecom-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
